[tool.pytest.ini_options]
testpaths = ["tools/agenda/tests"]
addopts = "-q --cov=tools/agenda --cov-report=term-missing --cov-report=xml --cov-fail-under=85"

[tool.coverage.run]
omit = [
  "tools/agenda/tests/*",
  "tests/*",
]
